name: ffmpeg_linux_pkg
description: "ffmpeg libary for linux platform and witter in dart.flutter linux package."
version: 0.0.1
repository: https://github.com/ThanCoder/ffmpeg_linux_pkg.git

environment:
  sdk: ^3.12.0

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.17.4
  ffi: ^2.1.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
